import { expect, test } from "@jest/globals";

// shows how the runner will run a javascript action with env / stdout protocol
test("this action works on my machine", () => {
  expect(true).toBe(true);
});
